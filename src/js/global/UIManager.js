//import NavbarDropdown from './UIElements/navbarDropdown';

export default () => {


  const hideUIElements = () => {
//    navbarDropdown.hide();
  };

  // I wanted to add 'onAction' or something like that
  // so I could easly manage the view whenever someone scrolls the book, click elements on page etc.
  // when those actions occur, we would like to hide modal for example, or hide navbarDropdown or hide explainableTooltips


  return {
//    hideUIElements,
  }
};
